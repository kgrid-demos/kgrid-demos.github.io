openapi: 3.0.0
info:
  version: 1.0.0
  title: 'CPIC - Genotype to Phenotype for CYP2D6'
  description: 'KGrid CPIC guidelines Genotype to Phenotype for CYP2D6'
servers:
  - url: 'http://kgrid.med.umich.edu/stack2'
paths:
  /shelf/ark:/99999/fk49z9gr7p:
    get:
      description: Retrieve the knowledge object.
      operationId: getko
      responses:
        '200':
          description: response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DosingRecOutput'
        default:
          description: unexpected error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /knowledgeObject/ark:/99999/fk49z9gr7p/result:
    post:
      description: Determine the pheno type based on the genotype.
      operationId: pheno
      requestBody:
        description: inputs
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/inputs'
      responses:
        '200':
          description: response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DosingRecOutput'
        default:
          description: unexpected error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
components:
  schemas:
    inputs:
      required:
        - allele1
        - allele2
        - diplotype
      properties:
        allele1:
          type: string
          example: '*1x2'
        allele2:
          type: string
          example: '*41'
        diplotype:
          type: string
          example: ''
    DosingRecOutput:
      required:
        - result
      properties:
        result:
          type: string
          example: 'metabolizer'
    Error:
      required:
        - code
        - message
      properties:
        code:
          type: integer
          format: int32
        message:
          type: string
