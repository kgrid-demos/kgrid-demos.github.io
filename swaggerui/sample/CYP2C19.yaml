openapi: 3.0.0
info:
  version: 1.0.0
  title: 'CPIC Guideline - CYP2C19'
  description: 'KGrid CPIC guidelines CYP2C19 Phenotype to Recommendation'
servers:
  - url: 'http://kgrid.med.umich.edu/stack2/knowledgeObject'
paths:
  /ark:/99999/fk4mc97w0h/result:
      post:
        description: Determine the pheno type based on the genotype of CYP2C19.
        operationId: pheno
        requestBody:
          description: Inputs of the genotype
          required: true
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/genoinputs'
        responses:
          '200':
            description: response
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/output'
          default:
            description: unexpected error
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/Error'
  /ark:/99999/fk4bz6hp15/result:
    post:
      description: Retrieve Clopidogrel Dosing Recommendation based on CYP2C19 pheno type.
      operationId: recomm
      requestBody:
        description: Input of phenotype
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/phenoinputs'
      responses:
        '200':
          description: response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/output'
        default:
          description: unexpected error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /ark:/99999/fk4d22836k/result:
    post:
      description: Retrieve Citalopram and Escitalopram Dosing Recommendation based on CYP2C19 pheno type.
      operationId: recomm
      requestBody:
        description: Input of phenotype
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/phenoinputs'
      responses:
        '200':
          description: response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/output'
        default:
          description: unexpected error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
components:
  schemas:
    genoinputs:
      required:
        - allele1
        - allele2
        - diplotype
      properties:
        allele1:
          type: string
          example: '*2'
        allele2:
          type: string
          example: '*3'
        diplotype:
          type: string
          example: ''
    phenoinputs:
      required:
        - phenotype
        - choice
      properties:
        phenotype:
          type: string
          enum: ['Ultrarapid metabolizer', 'Rapid metabolizer', 'Normal metabolizer','Likely Intermediate metabolizer', 'Intermediate metabolizer', 'Poor metabolizer', 'Likely Poor metabolizer']
          example: 'Poor metabolizer'
        choice:
          type: string
          enum: ['0','1','2']
          example: '1'
    output:
      required:
        - result
      properties:
        result:
          type: string
          example: 'Result'
    Error:
      required:
        - code
        - message
      properties:
        code:
          type: integer
          format: int32
        message:
          type: string
